import { DateIsoToDatePipe } from './date-iso-to-date.pipe';

describe('DateIsoToDatePipe', () => {
  it('create an instance', () => {
    const pipe = new DateIsoToDatePipe();
    expect(pipe).toBeTruthy();
  });
});
