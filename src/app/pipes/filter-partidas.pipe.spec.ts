import { FilterPartidasPipe } from './filter-partidas.pipe';

describe('FilterPartidasPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPartidasPipe();
    expect(pipe).toBeTruthy();
  });
});
